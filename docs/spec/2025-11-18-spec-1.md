# 📝 内容创作场景实用工具增强方案

## 🎯 需求分析

**AgentFlowCreativeHub** 是一个**多 Agent 协作内容创作平台**，核心 Agent 包括：
- Writer（写作）
- Reviewer（审校）
- Formatter（格式化）
- Translator（翻译）
- Analyzer（分析）
- Planner（规划）
- Researcher（研究）

**当前工具不足**：
- ❌ 计算器 - 对内容创作场景价值有限
- ❌ 通用搜索 - 已有知识库检索
- ❌ HTTP API - 对写作场景不够聚焦

**真正需要的工具**：
✅ 文本处理类（字数统计、格式转换）
✅ 内容质量检查类（语法检查、SEO 分析）
✅ 创作辅助类（关键词提取、摘要生成）
✅ 多语言支持类（术语查询、翻译记忆）

---

## 📋 新工具设计方案

### 优先级分类

**高优先级**（立即实施）：
1. 文本统计工具（字数、字符数、段落数）
2. 文本格式转换工具（Markdown ↔ HTML ↔ Plain Text）
3. 关键词提取工具（TF-IDF、TextRank）
4. 文本摘要工具（提取式摘要）

**中优先级**（按需实施）：
5. SEO 分析工具（关键词密度、可读性评分）
6. 文本比较工具（相似度计算、差异对比）
7. 术语库查询工具（翻译术语、专业词汇）

**低优先级**（可选）：
8. 敏感词检测工具
9. 文本去重工具
10. 引用格式化工具

---

## 🛠️ 详细设计

### 工具 1: 文本统计工具 ✨

**名称**: `text_statistics`

**功能**:
- 统计字数（中文字符、英文单词）
- 统计字符数（含/不含空格）
- 统计段落数、句子数
- 统计平均句长
- 估算阅读时间

**参数定义**:
```json
{
  "type": "object",
  "properties": {
    "text": {
      "type": "string",
      "description": "待统计的文本内容"
    },
    "language": {
      "type": "string",
      "enum": ["zh", "en", "auto"],
      "default": "auto",
      "description": "文本语言（auto=自动检测）"
    }
  },
  "required": ["text"]
}
```

**输出示例**:
```json
{
  "total_characters": 1250,
  "characters_no_spaces": 1000,
  "chinese_characters": 800,
  "english_words": 50,
  "paragraphs": 5,
  "sentences": 25,
  "avg_sentence_length": 40,
  "reading_time_minutes": 3.5
}
```

**应用场景**:
- Writer Agent: 检查文章是否达到目标字数
- Reviewer Agent: 评估内容长度和密度
- Formatter Agent: 判断是否需要分页

**实现要点**:
```go
// 中文字符统计（Unicode 范围：\u4e00-\u9fa5）
// 英文单词统计（按空格分割）
// 段落统计（按 \n\n 分割）
// 句子统计（按 。！？.!? 分割）
// 阅读时间（中文 300字/分钟，英文 200词/分钟）
```

---

### 工具 2: 文本格式转换工具 ✨

**名称**: `text_converter`

**功能**:
- Markdown → HTML
- HTML → Markdown
- Markdown → Plain Text
- HTML → Plain Text
- 代码高亮（Markdown 中的代码块）

**参数定义**:
```json
{
  "type": "object",
  "properties": {
    "text": {
      "type": "string",
      "description": "待转换的文本"
    },
    "from_format": {
      "type": "string",
      "enum": ["markdown", "html", "plain"],
      "description": "源格式"
    },
    "to_format": {
      "type": "string",
      "enum": ["markdown", "html", "plain"],
      "description": "目标格式"
    },
    "options": {
      "type": "object",
      "description": "转换选项（可选）",
      "properties": {
        "preserve_links": {"type": "boolean", "default": true},
        "preserve_images": {"type": "boolean", "default": true},
        "code_highlight": {"type": "boolean", "default": true}
      }
    }
  },
  "required": ["text", "from_format", "to_format"]
}
```

**输出示例**:
```json
{
  "converted_text": "<h1>标题</h1><p>内容...</p>",
  "format": "html",
  "success": true
}
```

**应用场景**:
- Formatter Agent: 自动转换文档格式
- Writer Agent: 将草稿转换为发布格式
- Reviewer Agent: 预览不同格式的内容

**实现方式**:
- 使用 `github.com/gomarkdown/markdown` - Markdown 解析
- 使用 `github.com/PuerkitoBio/goquery` - HTML 解析
- 使用 `github.com/alecthomas/chroma` - 代码高亮

---

### 工具 3: 关键词提取工具 ✨

**名称**: `keyword_extractor`

**功能**:
- TF-IDF 算法提取关键词
- TextRank 算法提取关键词
- 返回关键词及其权重
- 支持中英文

**参数定义**:
```json
{
  "type": "object",
  "properties": {
    "text": {
      "type": "string",
      "description": "待提取关键词的文本"
    },
    "top_k": {
      "type": "integer",
      "default": 10,
      "description": "返回前K个关键词"
    },
    "algorithm": {
      "type": "string",
      "enum": ["tfidf", "textrank", "both"],
      "default": "tfidf",
      "description": "提取算法"
    },
    "language": {
      "type": "string",
      "enum": ["zh", "en", "auto"],
      "default": "auto"
    }
  },
  "required": ["text"]
}
```

**输出示例**:
```json
{
  "keywords": [
    {"word": "人工智能", "score": 0.85},
    {"word": "机器学习", "score": 0.72},
    {"word": "深度学习", "score": 0.68}
  ],
  "algorithm_used": "tfidf",
  "total_keywords": 3
}
```

**应用场景**:
- Writer Agent: 自动生成标签
- Analyzer Agent: 分析文章主题
- SEO: 优化搜索关键词

**实现方式**:
- 使用 `github.com/go-ego/gse` - 中文分词
- 使用 `github.com/kljensen/snowball` - 英文词干提取
- 自实现 TF-IDF 和 TextRank 算法

---

### 工具 4: 文本摘要工具 ✨

**名称**: `text_summarizer`

**功能**:
- 提取式摘要（选择重要句子）
- 自动摘要生成（基于 LLM）
- 支持指定摘要长度
- 支持中英文

**参数定义**:
```json
{
  "type": "object",
  "properties": {
    "text": {
      "type": "string",
      "description": "待摘要的文本"
    },
    "method": {
      "type": "string",
      "enum": ["extractive", "llm"],
      "default": "extractive",
      "description": "摘要方法"
    },
    "max_sentences": {
      "type": "integer",
      "default": 3,
      "description": "最大句子数"
    },
    "max_length": {
      "type": "integer",
      "default": 200,
      "description": "最大字数"
    }
  },
  "required": ["text"]
}
```

**输出示例**:
```json
{
  "summary": "本文介绍了人工智能的发展历程。深度学习是当前最热门的研究方向。未来AI将在更多领域应用。",
  "method": "extractive",
  "sentences_used": 3,
  "total_length": 45
}
```

**应用场景**:
- Writer Agent: 自动生成文章摘要
- Reviewer Agent: 快速了解长文内容
- Planner Agent: 提取关键信息

**实现方式**:
- 提取式：TextRank 算法 + 句子重要性评分
- LLM 式：调用 AI 模型生成摘要

---

### 工具 5: SEO 分析工具 ✨

**名称**: `seo_analyzer`

**功能**:
- 关键词密度分析
- 可读性评分（Flesch Reading Ease）
- 标题标签检查
- Meta 描述检查
- 内链外链统计

**参数定义**:
```json
{
  "type": "object",
  "properties": {
    "text": {
      "type": "string",
      "description": "待分析的文本（HTML 或 Markdown）"
    },
    "target_keyword": {
      "type": "string",
      "description": "目标关键词（可选）"
    },
    "check_headings": {
      "type": "boolean",
      "default": true,
      "description": "是否检查标题结构"
    }
  },
  "required": ["text"]
}
```

**输出示例**:
```json
{
  "keyword_density": {
    "人工智能": 2.5,
    "机器学习": 1.8
  },
  "readability_score": 65,
  "readability_level": "易读",
  "headings": {
    "h1": 1,
    "h2": 5,
    "h3": 8
  },
  "links": {
    "internal": 10,
    "external": 3
  },
  "word_count": 1500,
  "recommendations": [
    "建议增加目标关键词密度",
    "H1 标题符合规范"
  ]
}
```

**应用场景**:
- Writer Agent: 实时 SEO 优化建议
- Analyzer Agent: 内容质量评估
- Reviewer Agent: SEO 审核

---

### 工具 6: 文本比较工具

**名称**: `text_comparator`

**功能**:
- 计算文本相似度（余弦相似度、编辑距离）
- 生成差异对比（Diff）
- 重复内容检测

**参数定义**:
```json
{
  "type": "object",
  "properties": {
    "text1": {"type": "string"},
    "text2": {"type": "string"},
    "method": {
      "type": "string",
      "enum": ["similarity", "diff", "duplicate"],
      "default": "similarity"
    }
  },
  "required": ["text1", "text2"]
}
```

**应用场景**:
- Reviewer Agent: 对比修改前后
- Writer Agent: 检查重复内容
- Translator Agent: 对比翻译质量

---

### 工具 7: 术语库查询工具

**名称**: `terminology_lookup`

**功能**:
- 查询专业术语翻译
- 查询术语定义
- 查询行业专用词汇
- 支持自定义术语库

**参数定义**:
```json
{
  "type": "object",
  "properties": {
    "term": {"type": "string", "description": "术语"},
    "source_lang": {"type": "string", "default": "zh"},
    "target_lang": {"type": "string", "default": "en"},
    "domain": {"type": "string", "description": "领域（可选）"}
  },
  "required": ["term"]
}
```

**应用场景**:
- Translator Agent: 专业翻译
- Writer Agent: 术语规范化
- Reviewer Agent: 术语一致性检查

---

## 📊 实施计划

### 阶段 1: 核心工具（高优先级）

**新增 4 个工具**:
1. ✅ `text_statistics` - 文本统计（~150 行）
2. ✅ `text_converter` - 格式转换（~200 行）
3. ✅ `keyword_extractor` - 关键词提取（~180 行）
4. ✅ `text_summarizer` - 文本摘要（~150 行）

**预计时间**: 4 小时
**预计代码**: ~680 行

### 阶段 2: 增强工具（中优先级）

**新增 3 个工具**:
5. ✅ `seo_analyzer` - SEO 分析（~200 行）
6. ✅ `text_comparator` - 文本比较（~120 行）
7. ✅ `terminology_lookup` - 术语查询（~100 行）

**预计时间**: 3 小时
**预计代码**: ~420 行

### 阶段 3: 优化现有工具

**调整**:
- ⚠️ **保留** `calculator` - 数据分析场景可能需要
- ⚠️ **保留** `web_search` - 研究场景需要
- ⚠️ **保留** `knowledge_search` - 核心功能
- ⚠️ **保留** `http_api` - 扩展性需要
- ✅ **新增** 内容创作专用工具

**总计**: 4 个原有工具 + 7 个新增工具 = **11 个工具**

---

## 🎯 预期成果

### 工具清单（完整版）

**通用工具**（4 个）:
- 🧮 calculator - 计算器
- 🔍 web_search - 网络搜索
- 📚 knowledge_search - 知识库检索
- 🌐 http_api - HTTP API 调用

**内容创作专用工具**（7 个）:
- 📊 text_statistics - 文本统计 ✨ 新增
- 🔄 text_converter - 格式转换 ✨ 新增
- 🏷️ keyword_extractor - 关键词提取 ✨ 新增
- 📝 text_summarizer - 文本摘要 ✨ 新增
- 🎯 seo_analyzer - SEO 分析 ✨ 新增
- 🔬 text_comparator - 文本比较 ✨ 新增
- 📖 terminology_lookup - 术语查询 ✨ 新增

**总计**: **11 个工具**

---

## 🚀 Agent 使用场景

### Writer Agent（写作）

**可用工具**:
- `text_statistics` - 检查字数达标
- `keyword_extractor` - 生成标签
- `text_summarizer` - 生成摘要
- `seo_analyzer` - SEO 优化建议
- `knowledge_search` - 查询资料

**工作流程**:
```
1. knowledge_search - 查询背景资料
2. [写作过程]
3. text_statistics - 检查字数
4. keyword_extractor - 提取关键词
5. seo_analyzer - SEO 优化
6. text_summarizer - 生成摘要
```

### Reviewer Agent（审校）

**可用工具**:
- `text_statistics` - 评估长度
- `text_comparator` - 对比版本
- `seo_analyzer` - SEO 审核
- `keyword_extractor` - 关键词检查

**工作流程**:
```
1. text_statistics - 统计基本信息
2. [审校过程]
3. text_comparator - 对比修改
4. seo_analyzer - SEO 评分
```

### Formatter Agent（格式化）

**可用工具**:
- `text_converter` - 格式转换
- `text_statistics` - 检查分段
- `seo_analyzer` - 标题结构检查

**工作流程**:
```
1. text_converter(markdown → html)
2. [格式调整]
3. text_statistics - 验证格式
```

### Translator Agent（翻译）

**可用工具**:
- `terminology_lookup` - 术语查询
- `text_comparator` - 翻译质量对比
- `text_statistics` - 长度对比

**工作流程**:
```
1. terminology_lookup - 查询专业术语
2. [翻译过程]
3. text_comparator - 对比原文
4. text_statistics - 验证长度
```

### Analyzer Agent（分析）

**可用工具**:
- `text_statistics` - 统计分析
- `keyword_extractor` - 主题分析
- `seo_analyzer` - 质量分析
- `text_summarizer` - 提取要点

**工作流程**:
```
1. text_statistics - 基础统计
2. keyword_extractor - 关键词分析
3. seo_analyzer - 质量评分
4. text_summarizer - 生成报告
```

---

## 📝 实施优先级建议

### 立即实施（本次）

**阶段 1 - 核心工具**:
1. ✅ `text_statistics` - 最基础、最实用
2. ✅ `text_converter` - 格式转换是刚需
3. ✅ `keyword_extractor` - SEO 和标签生成
4. ✅ `text_summarizer` - 自动摘要生成

**理由**: 这 4 个工具覆盖了内容创作的核心需求，立即可用。

### 后续扩展（按需）

**阶段 2 - 增强工具**:
5. `seo_analyzer` - SEO 优化（如需）
6. `text_comparator` - 版本对比（如需）
7. `terminology_lookup` - 专业翻译（如需）

**理由**: 这些工具提供增强能力，可根据实际使用反馈决定优先级。

---

## 🎉 预期效果

### 实施后系统能力

**Function Calling 工具系统**:
- 11 个工具（4 通用 + 7 内容创作）
- 完整覆盖内容创作工作流
- Agent 自动调用工具
- 多轮对话支持

**Agent 能力提升**:
- Writer Agent: 自动统计字数、生成摘要、SEO 优化
- Reviewer Agent: 自动对比版本、质量评分
- Formatter Agent: 自动格式转换
- Translator Agent: 术语查询、翻译对比
- Analyzer Agent: 全面内容分析

**项目完成度**: **90% → 95%** ✅

---

## 📋 技术实现要点

### 依赖库

```go
// 文本处理
"github.com/gomarkdown/markdown" // Markdown 解析
"github.com/PuerkitoBio/goquery" // HTML 解析
"github.com/alecthomas/chroma"   // 代码高亮

// 中文处理
"github.com/go-ego/gse"          // 中文分词
"github.com/yanyiwu/gojieba"     // 结巴分词（备选）

// 英文处理
"github.com/kljensen/snowball"   // 英文词干提取
"github.com/bbalet/stopwords"    // 停用词过滤

// 文本分析
"gonum.org/v1/gonum/mat"         // 矩阵运算（TF-IDF）
```

### 性能要求

| 工具 | 预期耗时 | 最大文本长度 |
|------|---------|-------------|
| text_statistics | < 10ms | 50K 字符 |
| text_converter | < 50ms | 100K 字符 |
| keyword_extractor | < 200ms | 50K 字符 |
| text_summarizer | < 500ms | 50K 字符 |
| seo_analyzer | < 300ms | 100K 字符 |

---

**准备好实施这些真正实用的内容创作工具了吗？** 🚀📝✨

这些工具将让 AgentFlowCreativeHub 的 Agent 真正具备专业的内容创作和分析能力！