# 🏗️ 项目构建指南

> **作者**: BaSui 😎 | **更新**: 2025-11-13

---

## 📋 目录

- [环境要求](#环境要求)
- [环境搭建](#环境搭建)
- [依赖安装](#依赖安装)
- [本地开发](#本地开发)
- [构建打包](#构建打包)
- [常见问题](#常见问题)

---

## 🎯 环境要求

### 必需软件

| 软件 | 版本要求 | 用途 | 下载地址 |
|------|---------|------|---------|
| **Go** | >= 1.21 | 后端服务开发 | [golang.org](https://golang.org/dl/) |
| **Python** | >= 3.10 | Agent服务开发 | [python.org](https://www.python.org/downloads/) |
| **Node.js** | >= 18.x | 前端开发 | [nodejs.org](https://nodejs.org/) |
| **PostgreSQL** | >= 14 | 主数据库 | [postgresql.org](https://www.postgresql.org/download/) |
| **Redis** | >= 6.0 | 缓存和会话 | [redis.io](https://redis.io/download) |
| **Docker** | >= 20.10 | 容器化部署（可选） | [docker.com](https://www.docker.com/get-started) |
| **Docker Compose** | >= 2.0 | 容器编排（可选） | [docs.docker.com](https://docs.docker.com/compose/install/) |

### 可选软件

| 软件 | 版本要求 | 用途 |
|------|---------|------|
| **RabbitMQ** | >= 3.11 | 消息队列 |
| **Milvus/Qdrant** | Latest | 向量数据库 |
| **Git** | >= 2.30 | 版本控制 |
| **Make** | >= 4.0 | 构建工具 |

### 系统要求

- **操作系统**：Linux / macOS / Windows 10+
- **内存**：>= 8GB（推荐16GB）
- **磁盘空间**：>= 20GB
- **CPU**：>= 4核（推荐8核）

---

## 🚀 环境搭建

### 1. 安装Go环境

#### Linux / macOS

```bash
# 下载Go
wget https://golang.org/dl/go1.21.0.linux-amd64.tar.gz

# 解压
sudo tar -C /usr/local -xzf go1.21.0.linux-amd64.tar.gz

# 配置环境变量
echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
echo 'export GOPATH=$HOME/go' >> ~/.bashrc
source ~/.bashrc

# 验证安装
go version
```

#### Windows

```powershell
# 下载并安装Go安装包
# https://golang.org/dl/go1.21.0.windows-amd64.msi

# 验证安装
go version
```

#### 配置Go代理（国内用户）

```bash
go env -w GOPROXY=https://goproxy.cn,direct
go env -w GO111MODULE=on
```

---

### 2. 安装Python环境

#### Linux / macOS

```bash
# 使用pyenv管理Python版本（推荐）
curl https://pyenv.run | bash

# 安装Python 3.10
pyenv install 3.10.0
pyenv global 3.10.0

# 验证安装
python --version
```

#### Windows

```powershell
# 下载并安装Python安装包
# https://www.python.org/downloads/

# 验证安装
python --version
```

#### 配置pip镜像（国内用户）

```bash
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

---

### 3. 安装Node.js环境

#### Linux / macOS

```bash
# 使用nvm管理Node.js版本（推荐）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 安装Node.js 18
nvm install 18
nvm use 18

# 验证安装
node --version
npm --version
```

#### Windows

```powershell
# 下载并安装Node.js安装包
# https://nodejs.org/

# 验证安装
node --version
npm --version
```

#### 配置npm镜像（国内用户）

```bash
npm config set registry https://registry.npmmirror.com
```

---

### 4. 安装数据库

#### PostgreSQL

**Linux (Ubuntu/Debian)**

```bash
# 安装PostgreSQL
sudo apt update
sudo apt install postgresql postgresql-contrib

# 启动服务
sudo systemctl start postgresql
sudo systemctl enable postgresql

# 创建数据库和用户
sudo -u postgres psql
CREATE DATABASE creative_hub;
CREATE USER hub_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE creative_hub TO hub_user;
\q
```

**macOS**

```bash
# 使用Homebrew安装
brew install postgresql@14

# 启动服务
brew services start postgresql@14

# 创建数据库
createdb creative_hub
```

**Windows**

```powershell
# 下载并安装PostgreSQL安装包
# https://www.postgresql.org/download/windows/

# 使用pgAdmin创建数据库
```

#### Redis

**Linux (Ubuntu/Debian)**

```bash
# 安装Redis
sudo apt update
sudo apt install redis-server

# 启动服务
sudo systemctl start redis-server
sudo systemctl enable redis-server

# 验证安装
redis-cli ping
```

**macOS**

```bash
# 使用Homebrew安装
brew install redis

# 启动服务
brew services start redis
```

**Windows**

```powershell
# 下载Redis for Windows
# https://github.com/microsoftarchive/redis/releases

# 或使用Docker
docker run -d -p 6379:6379 redis:latest
```

---

### 5. 安装Docker（可选）

#### Linux

```bash
# 安装Docker
curl -fsSL https://get.docker.com | bash

# 启动Docker
sudo systemctl start docker
sudo systemctl enable docker

# 添加当前用户到docker组
sudo usermod -aG docker $USER

# 安装Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 验证安装
docker --version
docker-compose --version
```

#### macOS / Windows

```bash
# 下载并安装Docker Desktop
# https://www.docker.com/products/docker-desktop

# 验证安装
docker --version
docker-compose --version
```

---

## 📦 依赖安装

### 1. 克隆项目

```bash
# 克隆仓库
git clone https://github.com/yourusername/AgentFlowCreativeHub.git
cd AgentFlowCreativeHub
```

---

### 2. 后端依赖（Go）

```bash
# 进入后端目录
cd backend

# 下载依赖
go mod download

# 验证依赖
go mod verify

# 整理依赖
go mod tidy
```

**主要依赖包**：

```go
// go.mod 示例
module github.com/yourusername/AgentFlowCreativeHub

go 1.21

require (
    github.com/gin-gonic/gin v1.9.1          // Web框架
    github.com/lib/pq v1.10.9                // PostgreSQL驱动
    github.com/go-redis/redis/v8 v8.11.5     // Redis客户端
    github.com/golang-jwt/jwt/v5 v5.0.0      // JWT认证
    github.com/spf13/viper v1.16.0           // 配置管理
    go.uber.org/zap v1.25.0                  // 日志库
    gorm.io/gorm v1.25.4                     // ORM框架
    gorm.io/driver/postgres v1.5.2           // GORM PostgreSQL驱动
)
```

---

### 3. Agent服务依赖（Python）

```bash
# 进入Agent服务目录
cd agent-service

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Linux/macOS
source venv/bin/activate
# Windows
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
```

**requirements.txt 示例**：

```txt
# Web框架
fastapi==0.103.0
uvicorn[standard]==0.23.2

# AI模型
openai==1.3.0
anthropic==0.5.0
langchain==0.0.300

# 向量数据库
pymilvus==2.3.0
qdrant-client==1.6.0

# 数据处理
numpy==1.24.3
pandas==2.0.3

# 工具库
pydantic==2.4.0
python-dotenv==1.0.0
httpx==0.25.0
```

---

### 4. 前端依赖（Node.js）

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 或使用yarn
yarn install

# 或使用pnpm（推荐）
pnpm install
```

**package.json 主要依赖**：

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.16.0",
    "antd": "^5.10.0",
    "@reduxjs/toolkit": "^1.9.7",
    "react-redux": "^8.1.3",
    "@tanstack/react-query": "^5.0.0",
    "axios": "^1.5.1",
    "dayjs": "^1.11.10"
  },
  "devDependencies": {
    "@types/react": "^18.2.31",
    "@types/react-dom": "^18.2.14",
    "@vitejs/plugin-react": "^4.1.0",
    "typescript": "^5.2.2",
    "vite": "^4.5.0",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3"
  }
}
```

---

## 💻 本地开发

### 1. 配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑环境变量
vim .env
```

**.env 示例**：

```bash
# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_NAME=creative_hub
DB_USER=hub_user
DB_PASSWORD=your_password

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT配置
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRE=24h

# AI模型配置
OPENAI_API_KEY=sk-xxx
ANTHROPIC_API_KEY=sk-ant-xxx

# 向量数据库配置
MILVUS_HOST=localhost
MILVUS_PORT=19530

# 服务端口
API_PORT=8080
AGENT_PORT=8081
FRONTEND_PORT=3000
```

---

### 2. 初始化数据库

```bash
# 运行数据库迁移脚本
cd backend
go run cmd/migrate/main.go

# 或使用make命令
make migrate
```

---

### 3. 启动后端服务

```bash
# 进入后端目录
cd backend

# 开发模式启动
go run main.go

# 或使用air热重载（推荐）
air

# 或使用make命令
make run
```

**验证后端服务**：

```bash
# 健康检查
curl http://localhost:8080/health

# 查看API文档
open http://localhost:8080/swagger
```

---

### 4. 启动Agent服务

```bash
# 进入Agent服务目录
cd agent-service

# 激活虚拟环境
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate     # Windows

# 启动服务
python main.py

# 或使用uvicorn
uvicorn main:app --reload --port 8081
```

**验证Agent服务**：

```bash
# 健康检查
curl http://localhost:8081/health

# 查看API文档
open http://localhost:8081/docs
```

---

### 5. 启动前端服务

```bash
# 进入前端目录
cd frontend

# 启动开发服务器
npm run dev

# 或使用yarn
yarn dev

# 或使用pnpm
pnpm dev
```

**访问前端**：

```bash
open http://localhost:3000
```

---

### 6. 使用Docker Compose（推荐）

```bash
# 一键启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down

# 重启服务
docker-compose restart
```

**docker-compose.yml 示例**：

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_DB: creative_hub
      POSTGRES_USER: hub_user
      POSTGRES_PASSWORD: your_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  backend:
    build: ./backend
    ports:
      - "8080:8080"
    depends_on:
      - postgres
      - redis
    environment:
      - DB_HOST=postgres
      - REDIS_HOST=redis

  agent-service:
    build: ./agent-service
    ports:
      - "8081:8081"
    depends_on:
      - backend

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  postgres_data:
```

---

## 📦 构建打包

### 1. 后端构建

```bash
# 进入后端目录
cd backend

# 构建二进制文件
go build -o bin/server main.go

# 或使用make命令
make build

# 交叉编译（Linux）
GOOS=linux GOARCH=amd64 go build -o bin/server-linux main.go

# 交叉编译（Windows）
GOOS=windows GOARCH=amd64 go build -o bin/server.exe main.go

# 构建Docker镜像
docker build -t creative-hub-backend:latest .
```

**Dockerfile 示例**：

```dockerfile
# 构建阶段
FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o server main.go

# 运行阶段
FROM alpine:latest
RUN apk --no-cache add ca-certificates
WORKDIR /root/
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["./server"]
```

---

### 2. Agent服务构建

```bash
# 进入Agent服务目录
cd agent-service

# 构建Docker镜像
docker build -t creative-hub-agent:latest .
```

**Dockerfile 示例**：

```dockerfile
FROM python:3.10-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
EXPOSE 8081
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8081"]
```

---

### 3. 前端构建

```bash
# 进入前端目录
cd frontend

# 构建生产版本
npm run build

# 或使用yarn
yarn build

# 或使用pnpm
pnpm build

# 预览构建结果
npm run preview

# 构建Docker镜像
docker build -t creative-hub-frontend:latest .
```

**Dockerfile 示例**：

```dockerfile
# 构建阶段
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# 运行阶段
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

---

## 🐛 常见问题

### 1. Go依赖下载失败

**问题**：`go mod download` 超时或失败

**解决方案**：

```bash
# 配置Go代理
go env -w GOPROXY=https://goproxy.cn,direct

# 或使用其他代理
go env -w GOPROXY=https://goproxy.io,direct
```

---

### 2. Python依赖安装失败

**问题**：`pip install` 失败或超时

**解决方案**：

```bash
# 配置pip镜像
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple

# 或临时使用镜像
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

### 3. Node.js依赖安装失败

**问题**：`npm install` 失败或超时

**解决方案**：

```bash
# 配置npm镜像
npm config set registry https://registry.npmmirror.com

# 清除缓存
npm cache clean --force

# 重新安装
npm install
```

---

### 4. 数据库连接失败

**问题**：无法连接到PostgreSQL

**解决方案**：

```bash
# 检查PostgreSQL服务状态
sudo systemctl status postgresql

# 检查端口是否开放
netstat -an | grep 5432

# 检查防火墙
sudo ufw allow 5432

# 测试连接
psql -h localhost -U hub_user -d creative_hub
```

---

### 5. 端口被占用

**问题**：启动服务时提示端口被占用

**解决方案**：

```bash
# 查看端口占用情况
# Linux/macOS
lsof -i :8080

# Windows
netstat -ano | findstr :8080

# 杀死占用端口的进程
# Linux/macOS
kill -9 <PID>

# Windows
taskkill /PID <PID> /F

# 或修改配置文件使用其他端口
```

---

### 6. Docker构建失败

**问题**：Docker镜像构建失败

**解决方案**：

```bash
# 清理Docker缓存
docker system prune -a

# 使用--no-cache重新构建
docker build --no-cache -t creative-hub-backend:latest .

# 检查Docker磁盘空间
docker system df
```

---

### 7. 前端页面空白

**问题**：前端启动后页面空白

**解决方案**：

```bash
# 检查浏览器控制台错误
# 清除浏览器缓存
# 检查API地址配置

# 重新安装依赖
rm -rf node_modules package-lock.json
npm install

# 检查环境变量
cat .env
```

---

### 8. 热重载不生效

**问题**：修改代码后不自动重启

**解决方案**：

**Go后端**：

```bash
# 安装air
go install github.com/cosmtrek/air@latest

# 使用air启动
air
```

**Python Agent**：

```bash
# 使用uvicorn的--reload选项
uvicorn main:app --reload --port 8081
```

**前端**：

```bash
# 检查vite.config.ts配置
# 确保server.watch配置正确
```

---

## 📚 相关文档

- [需求分析文档](需求分析文档.md)
- [架构设计文档](架构设计文档.md)
- [API接口文档](API接口文档.md)
- [部署运维文档](部署运维文档.md)
- [开发规范文档](开发规范文档.md)

---

## 💡 开发技巧

### 1. 使用Makefile简化命令

```makefile
# Makefile 示例
.PHONY: help install run build test clean

help:
	@echo "可用命令："
	@echo "  make install  - 安装所有依赖"
	@echo "  make run      - 启动所有服务"
	@echo "  make build    - 构建所有服务"
	@echo "  make test     - 运行所有测试"
	@echo "  make clean    - 清理构建文件"

install:
	cd backend && go mod download
	cd agent-service && pip install -r requirements.txt
	cd frontend && npm install

run:
	docker-compose up -d

build:
	cd backend && go build -o bin/server main.go
	cd agent-service && docker build -t agent:latest .
	cd frontend && npm run build

test:
	cd backend && go test ./...
	cd agent-service && pytest
	cd frontend && npm run test

clean:
	rm -rf backend/bin
	rm -rf agent-service/__pycache__
	rm -rf frontend/dist
```

---

### 2. 使用脚本自动化

```bash
#!/bin/bash
# scripts/dev.sh - 一键启动开发环境

echo "🚀 启动开发环境..."

# 启动数据库
docker-compose up -d postgres redis

# 等待数据库就绪
sleep 5

# 启动后端
cd backend && go run main.go &

# 启动Agent服务
cd agent-service && source venv/bin/activate && python main.py &

# 启动前端
cd frontend && npm run dev &

echo "✅ 所有服务已启动！"
```

---

## 🎉 完成！

恭喜！你已经成功搭建了开发环境！🎊

如果遇到问题，请查看 [常见问题](#常见问题) 或提交 [Issue](https://github.com/yourusername/multi-agent-creative-hub/issues)。

**Happy Coding! 💻✨**
