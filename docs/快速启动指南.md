# 🚀 AgentFlowCreativeHub - 快速启动指南

> **更新日期**: 2025-11-17  
> **状态**: Sprint 1 任务 1.1 完成 ✅

---

## 📋 前置要求

### 必需软件

1. **Go 1.21+**
   ```bash
   # 检查 Go 版本
   go version
   
   # Windows 安装：https://go.dev/dl/
   # 下载 go1.21.x.windows-amd64.msi 并安装
   ```

2. **PostgreSQL 14+**
   ```bash
   # Windows 安装：https://www.postgresql.org/download/windows/
   # 或使用 Docker
   docker run -d \
     --name agentflow-postgres \
     -e POSTGRES_PASSWORD=postgres \
     -e POSTGRES_DB=agentflow_dev \
     -p 5432:5432 \
     postgres:14
   ```

3. **Redis（可选，用于缓存）**
   ```bash
   # Docker 安装
   docker run -d \
     --name agentflow-redis \
     -p 6379:6379 \
     redis:7
   ```

---

## 🔧 配置步骤

### 1. 安装 Go 依赖

```bash
cd D:\code\AgentFlowCreativeHub\backend
go mod tidy
```

### 2. 配置环境变量

复制环境变量模板并填写实际值：

```bash
# 复制模板
copy .env.example .env

# 编辑 .env 文件，填写以下配置
APP_ENV=dev
APP_DATABASE_HOST=localhost
APP_DATABASE_PORT=5432
APP_DATABASE_USER=postgres
APP_DATABASE_PASSWORD=postgres
APP_DATABASE_DBNAME=agentflow_dev

# OpenAI API Key（如果需要）
OPENAI_API_KEY=sk-your-key-here

# Anthropic API Key（如果需要）
ANTHROPIC_API_KEY=sk-ant-your-key-here
```

### 3. 创建数据库

```sql
-- 连接 PostgreSQL
psql -U postgres

-- 创建数据库
CREATE DATABASE agentflow_dev;

-- 退出
\q
```

### 4. 运行数据库迁移

```bash
# 方法 1: 使用 psql 执行迁移脚本
cd D:\code\AgentFlowCreativeHub
psql -U postgres -d agentflow_dev -f db/migrations/0001_multi_tenant.sql
psql -U postgres -d agentflow_dev -f db/migrations/0002_rag_tool_system.sql
psql -U postgres -d agentflow_dev -f db/migrations/0003_add_soft_delete.sql
psql -U postgres -d agentflow_dev -f db/migrations/0004_add_triggers.sql

# 方法 2: 使用 migrate 工具（推荐）
# 安装 migrate
go install -tags 'postgres' github.com/golang-migrate/migrate/v4/cmd/migrate@latest

# 执行迁移
migrate -path db/migrations -database "postgresql://postgres:postgres@localhost:5432/agentflow_dev?sslmode=disable" up
```

---

## ▶️ 启动应用

### 开发模式

```bash
cd D:\code\AgentFlowCreativeHub\backend

# 设置环境变量（PowerShell）
$env:APP_ENV="dev"

# 运行应用
go run cmd/server/main.go
```

### 预期输出

```
2025-11-17T23:30:00.000+0800    INFO    logger/logger.go:50    应用启动中...    {"env": "dev", "mode": "debug"}
2025-11-17T23:30:00.100+0800    INFO    infra/database.go:57   数据库连接成功  {"host": "localhost", "port": 5432, "database": "agentflow_dev"}
2025-11-17T23:30:00.150+0800    INFO    main.go:75             HTTP 服务器启动  {"port": 8080}
```

---

## ✅ 验证服务

### 健康检查

```bash
# PowerShell
Invoke-WebRequest http://localhost:8080/health

# 预期响应
{
  "status": "healthy",
  "service": "AgentFlowCreativeHub"
}
```

### 就绪检查

```bash
# PowerShell
Invoke-WebRequest http://localhost:8080/ready

# 预期响应
{
  "status": "ready",
  "database": "connected"
}
```

### API 测试

```bash
# 测试租户 API（占位符）
Invoke-WebRequest http://localhost:8080/api/tenants

# 预期响应
{
  "message": "List tenants - TODO"
}
```

---

## 📁 项目结构

```
AgentFlowCreativeHub/
├── backend/
│   ├── cmd/
│   │   └── server/
│   │       └── main.go           # 主程序入口 ✅
│   ├── internal/
│   │   ├── config/
│   │   │   └── config.go         # 配置管理 ✅
│   │   ├── logger/
│   │   │   └── logger.go         # 日志系统 ✅
│   │   ├── infra/
│   │   │   ├── database.go       # 数据库连接 ✅
│   │   │   └── gorm_logger.go    # GORM 日志适配器 ✅
│   │   ├── tenant/               # 租户模块 ✅
│   │   ├── workflow/             # 工作流模块 ✅
│   │   ├── agent/                # Agent 模块 ✅
│   │   ├── template/             # 模板模块 ✅
│   │   └── models/               # AI 模型模块 ✅
│   ├── api/
│   │   ├── setup.go              # 路由设置 ✅
│   │   └── router.go             # 原有路由（租户） ✅
│   └── go.mod                    # Go 依赖 ✅
├── config/
│   ├── dev.yaml                  # 开发环境配置 ✅
│   └── prod.yaml                 # 生产环境配置 ✅
├── db/
│   └── migrations/               # 数据库迁移脚本 ✅
│       ├── 0001_multi_tenant.sql
│       ├── 0002_rag_tool_system.sql
│       ├── 0003_add_soft_delete.sql
│       └── 0004_add_triggers.sql
└── .env.example                  # 环境变量模板 ✅
```

---

## 🐛 常见问题

### Q1: go: command not found

**解决**：安装 Go 1.21+
- Windows: https://go.dev/dl/
- 安装后重启终端

### Q2: 数据库连接失败

**检查**：
```bash
# 检查 PostgreSQL 是否运行
psql -U postgres -c "SELECT version();"

# 检查连接信息
# .env 中的 APP_DATABASE_* 配置是否正确
```

### Q3: 端口 8080 被占用

**解决**：
```yaml
# 修改 config/dev.yaml
server:
  port: 8081  # 改为其他端口
```

### Q4: 迁移脚本执行失败

**解决**：
```bash
# 检查数据库是否已创建
psql -U postgres -l | grep agentflow_dev

# 手动创建
psql -U postgres -c "CREATE DATABASE agentflow_dev;"
```

---

## 📝 下一步

### ✅ 已完成（Sprint 1 - 任务 1.1）

- ✅ Go 依赖管理（go.mod）
- ✅ 配置管理系统（Viper）
- ✅ 日志系统（Zap）
- ✅ 数据库连接（GORM + PostgreSQL）
- ✅ 主程序入口（cmd/server/main.go）
- ✅ 基础路由（健康检查、占位符 API）

### 🔜 待实现（Sprint 1 - 任务 1.2-1.3）

1. **补全 Service 层**（2 天）
   - ModelService - AI 模型管理
   - TemplateService - Prompt 模板管理
   - AgentService - Agent 配置管理
   - WorkflowService - 工作流管理

2. **补全 API 路由层**（1.5 天）
   - Models API
   - Templates API
   - Agents API
   - Workflows API

### 📚 相关文档

- [项目计划文档](./项目计划文档.md)
- [数据模型补充完成总结](./数据模型补充完成总结.md)
- [下一步实施计划](../.factory/docs/2025-11-17-multiagent-creative-hub-v2.md)

---

## 🎉 总结

Sprint 1 任务 1.1 已完成！现在你已经有：

✅ 完整的配置管理系统  
✅ 结构化日志系统  
✅ 数据库连接和连接池  
✅ 可启动的 HTTP 服务器  
✅ 健康检查和就绪检查  
✅ 基础路由框架  

**服务器已可以启动并响应请求！** 🚀

接下来我们将实现核心 Service 层和 API 路由，让系统真正可用。
